server:
  port: 8888
spring:
  application:
    name: WELCOME-API
  boot:
    admin:
      client:
        url: http://localhost:7777/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'
